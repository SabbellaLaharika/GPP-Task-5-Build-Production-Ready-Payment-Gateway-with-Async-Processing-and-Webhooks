# ========================================
# Payment Gateway - Docker Configuration
# ========================================
# Copy this file to .env and customize for your deployment
# Usage: cp .env.example .env

# ========================================
# DATABASE CONFIGURATION
# ========================================
POSTGRES_DB=payment_gateway
POSTGRES_USER=gateway_user
POSTGRES_PASSWORD=gateway_pass

# External port for accessing PostgreSQL from host machine
POSTGRES_PORT=5440

# ========================================
# APPLICATION PORTS
# ========================================
BACKEND_PORT=8000
FRONTEND_PORT=3000
CHECKOUT_PORT=3001

# ========================================
# BACKEND ENVIRONMENT VARIABLES
# ========================================
# Database connection (used by Spring Boot)
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/payment_gateway
SPRING_DATASOURCE_USERNAME=gateway_user
SPRING_DATASOURCE_PASSWORD=gateway_pass

# Database schema management: update (recommended) or create-drop (resets data)
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# ========================================
# DOCKER HUB IMAGES (OPTIONAL)
# ========================================
# Docker Hub username (change if using your own images)
DOCKER_USERNAME=sabbellalaharika

# Image tags (change for versioning)
BACKEND_IMAGE_TAG=latest
FRONTEND_IMAGE_TAG=latest
CHECKOUT_IMAGE_TAG=latest

# ========================================
# NOTES
# ========================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your values
# 3. Add .env to .gitignore (never commit secrets!)
# 4. For production, change passwords and use strong values
# 5. Default values work for development/testing